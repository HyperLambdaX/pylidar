# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info/
*.so
*.pyd
.pytest_cache/
.benchmarks/

# Virtual environments
.venv/

# macOS
.DS_Store

# Editor / IDE
.idea/
.vscode/

# Claude Code workspace (per-machine, not project content)
.agents/
.claude/
skills-lock.json

# Per-agent guide files (kept locally for each tool's own consumption,
# not shared via git — different machines / collaborators may want
# different versions)
CLAUDE.md
AGENTS.md

# planning-with-files scratch (per-session, not project content)
task_plan.md
findings.md
progress.md
.planning/
.plan-attestation

# Internal spec / planning docs (kept locally only)
rd.md
docs/superpowers/specs/

# scikit-build-core editable redirect cache
_skbuild/

# CMake user/build artifacts that may land outside build/
CMakeUserPresets.json
compile_commands.json
